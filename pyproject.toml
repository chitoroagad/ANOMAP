[project]
name = "peerwatch"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.pyrefly]
python-platform = "linux"
project-includes = ["**/*.py*", "**/*.ipynb"]
